# Compatibility entrypoint for tools that still run `pip install -r`.
# The complete transitive runtime set is generated from pyproject.toml and
# pinned in requirements.lock; Docker installs that file directly.
-r requirements.lock
